name: sendsar_uikit_example
description: Sample app for sendsar_chat_uikit — mirrors sendsar-uikit-angular sample-app.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  sendsar_chat: ^0.5.0
  sendsar_chat_uikit:
    path: ../sendsar_chat_uikit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
